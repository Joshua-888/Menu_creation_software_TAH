{
  "name": "menu-creation-software-tah",
  "version": "0.1.0",
  "private": true,
  "description": "TakeAwayHero menu migration platform — deterministic domain engine first",
  "type": "module",
  "scripts": {
    "lint": "eslint src tests --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:domain": "vitest run tests/domain",
    "test:contract": "vitest run tests/contract",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test:domain && npm run test:contract",
    "discover:admin": "tsx scripts/discover-admin-readonly.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@types/node": "^22.13.10",
    "dotenv": "^16.4.7",
    "eslint": "^9.22.0",
    "fast-check": "^4.0.1",
    "playwright": "^1.51.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.26.1",
    "vitest": "^3.0.8"
  }
}
