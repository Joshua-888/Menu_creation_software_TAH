# TakeAwayHero Admin Contract

**Status:** stub until Milestone 2 (read-only discovery).

Will document:

- `data-admin-contract-version` / probe results  
- certified selectors (`getByTestId` preferred)  
- adapter lifecycle: DEVELOPMENT → TESTED → CERTIFIED → DEPRECATED  
- canary certification requirements  

Provisional observations from the master spec (unverified): `/admin/menu`, fields Menunummer/Navn/… — treat as hypotheses only.
