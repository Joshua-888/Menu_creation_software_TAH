# Railway — TakeAwayHero Menu Operator Portal
[build]
builder = "RAILPACK"
buildCommand = "npm run portal:build"

[deploy]
startCommand = "npm run portal:start"
healthcheckPath = "/login"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
