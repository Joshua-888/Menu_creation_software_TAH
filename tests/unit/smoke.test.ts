import { describe, expect, it } from "vitest";

/**
 * Placeholder so `npm run test:unit` has a target until later milestones.
 */
describe("unit smoke", () => {
  it("passes", () => {
    expect(true).toBe(true);
  });
});
